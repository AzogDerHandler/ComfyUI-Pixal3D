# Intentionally empty.
#
# This fork installs ALL dependencies into <pack>/vendor/ via install.py and
# never touches the host Python env. See vendor_requirements.txt for the
# actual dependency lock, and README.md for install instructions.
