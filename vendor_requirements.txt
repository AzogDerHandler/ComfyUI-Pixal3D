# Pure-Python / PyPI dependency lock for the vendor/ folder.
# Installed by install.py with `pip install --target vendor --no-deps`, so the
# FULL transitive closure is enumerated here on purpose — nothing in this file
# may pull torch, torchvision, numpy, or triton (those stay in the host env).
#
# Pins follow upstream TencentARC/Pixal3D's tested stack where one exists.

# --- ML stack -----------------------------------------------------------
transformers==4.57.3
diffusers==0.37.1
accelerate==1.13.0
kornia==0.8.2
timm==1.0.22
safetensors>=0.7.0          # <0.7.0 fails on Pixal3D's ss_flow checkpoint
huggingface_hub>=0.34.0
hf_transfer
hf_xet

# --- camera estimation (MoGe-2, pinned to upstream Pixal3D's commit) -----
moge @ git+https://github.com/microsoft/MoGe.git@07444410f1e33f402353b99d6ccd26bd31e469e8
utils3d @ git+https://github.com/EasternJournalist/utils3d.git@3fab839f0be9931dac7c8488eb0e1600c236e183

# --- imaging / mesh ------------------------------------------------------
opencv-python-headless>=4.10,<4.13
pillow>=10.0.0
imageio>=2.31.0
imageio-ffmpeg
trimesh>=4.5.0
plyfile
mapbox-earcut

# --- misc ----------------------------------------------------------------
easydict
tqdm
einops
psutil
zstandard
scipy>=1.11
networkx

# --- transitive closure (spelled out because of --no-deps) ---------------
tokenizers>=0.21
regex
pyyaml
filelock
fsspec
packaging
typing_extensions
requests
charset-normalizer
idna
urllib3
certifi
kornia_rs
importlib_metadata
zipp
